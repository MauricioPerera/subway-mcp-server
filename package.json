{
  "name": "subway-mcp-server",
  "version": "1.0.0",
  "description": "Local MCP server that delegates tasks to codex, claude, and agy CLI sub-agents.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test test/*.test.mjs",
    "coverage": "npm run build && c8 node --test test/*.test.mjs",
    "coverage:badge": "node scripts/make-coverage-badge.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "c8": {
    "all": true,
    "include": ["dist/**/*.js"],
    "exclude": ["dist/**/*.d.ts"],
    "reporter": ["text", "json-summary"]
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "c8": "^11.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
